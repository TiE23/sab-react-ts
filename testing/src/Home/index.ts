/**
 * Using index.ts files is a great way to combine closely related components
 * into a single encompassing component to be used together. Smaller components
 * and other utility modules then become slightly obfuscated from the normal
 * import structure.
 * That, and it also helps with organization work without needing to rely on
 * long prefixes in your file names.
 * Something interesting to consider in the future!
 */
export * from './Home'